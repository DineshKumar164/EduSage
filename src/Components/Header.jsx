import React from 'react';
import './Header.css';

const Header = () => (
  <header className="header">
    <nav>
      <h1>Edusage.</h1>
      <button>Action1.</button>
    </nav>
  </header>
);

export default Header;